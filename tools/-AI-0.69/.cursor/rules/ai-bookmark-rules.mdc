---
description: 神灯AI书签管理器规则
globs: 
alwaysApply: false
---

# 神灯AI书签管理器开发规则

## 一、安全修改规则

### 1. 资源本地化原则
✅ 将所有外部CDN资源（React等）下载到本地assets目录  
✅ 修改HTML引用时保持相对路径准确  
❌ 不要在HTML中直接引用任何外部域名的资源  

### 2. 代码修改最小化原则
✅ 使用注释方式暂时禁用可疑代码，而非直接删除  
✅ 保留原代码的函数和变量声明，哪怕只是空函数  
❌ 不要删除任何看似不相关的函数或变量定义  

### 3. 全局对象保护原则
✅ 使用条件检查包裹所有可能未定义的全局对象引用  
✅ 在操作DOM前总是检查元素是否存在  
❌ 不要重写已存在的全局函数和变量  

### 4. 执行顺序安全原则
✅ 将函数声明移到文件顶部，或使用函数表达式  
✅ 避免在声明前使用变量和函数  
❌ 不要依赖执行顺序或作用域提升  

### 5. 错误处理防御原则
✅ 使用try/catch包裹所有可能失败的操作  
✅ 为异步操作添加错误处理  
✅ 提供有意义的错误信息和后备方案  

### 6. 版本号更新原则
✅ 版本更新时只修改三个核心文件：manifest.json, nav-bar.js, home.html  
✅ 确保所有修改的文件中版本号保持一致  
❌ 不要在版本更新时修改功能代码或引入新bug  

## 二、代码分析指南

### 扩大分析范围
- 不仅分析单个文件，还要关注文件间的接口和依赖
- 特别关注事件处理和函数调用的跨文件传递

### 关注集成点
- 识别并验证所有全局变量、函数和事件的注册与使用
- 特别关注全局命名空间中的对象和函数

### 追踪完整执行路径
- 从用户交互开始，追踪到最终执行的全部路径
- 考虑不同场景下的执行流向

### 模拟错误情况
- 主动思考"如果这一步失败会怎样"
- 检查错误处理代码提示的潜在问题

## 三、代码生成和管理策略

### 1. 模块化代码生成
- 分解大型函数：将大型函数分解为更小的独立模块
- 每次生成完整函数：确保每个函数都是完整的，包括所有必要的闭合括号
- 按功能块分离：将相关的功能代码组织在一起，而不是一次生成整个文件

### 2. 括号平衡检查
- 使用括号计数器：在生成代码时维护括号计数器，确保每种括号（{}, (), []）都是平衡的
- 结构验证：为每个大型函数末尾添加注释以标记闭合位置，如：} // end of initFunction
- 嵌套层级限制：控制嵌套的深度，避免超过3-4层的嵌套结构

### 3. 代码生成策略
- 自底向上构建：先生成内部函数，再组装成更大的结构
- 使用临时文件：对于大型代码块，先生成到临时文件，验证无误后再整合
- 增量验证：每次添加一小部分代码后立即验证其语法正确性

### 4. 异步代码处理
- Promise链替代深层回调：使用Promise链或async/await代替深层回调结构
- 异步函数隔离：将复杂的异步逻辑提取到单独的命名函数中
- 命名回调函数：避免使用匿名函数作为回调，而是使用命名函数以增加可读性

### 5. 工具辅助
- 使用代码格式化工具：在生成代码后应用格式化，这常常能发现括号不匹配问题
- 引入linter检查：在生成关键部分后插入linter检查步骤
- 代码段验证：在生成大型代码之前，先用小样例验证生成逻辑

### 6. 版本更新检查清单
- manifest.json中的version字段已更新
- nav-bar.js中的version span已更新
- home.html中的version span已更新
- 所有更新的版本号保持一致
- 更新记录变更内容（如有）
- 测试各页面版本号显示是否正确
