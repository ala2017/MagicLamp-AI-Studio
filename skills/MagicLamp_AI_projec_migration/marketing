# 从零开始，换掉 AI 开发工具时如何不丢失上下文?

> 当 Cursor 幻觉改了你的代码，当 Windsurf 突然收费，当你想试试新出的 o3-mini 但会话已经长了——你面临的第一个问题不是"新工具好不好用"，而是"它根本不知道我做到哪了"。

## 一个被反复支付的隐性成本

在真实项目里换模型或换工具，新环境的默认策略几乎是灾难性的：

1. **全量盲扫**：把 `node_modules`、`.next`、日志、向量库一股脑读进上下文，Token 瞬间蒸发
2. **覆盖未提交代码**：因为没有 git 状态提示，新模型随手改文件，把你昨晚的改动直接覆盖
3. **找不到断点**：PRD 写了"支付模块待开发"，但模型不知道具体改哪个文件，只能反复试探
4. **密钥外泄**：源文件里的 API Key、数据库密码被截进上下文，进入云端日志

单次迁移的成本，往往不是"切换设置"的那几分钟，而是**几十轮无效对话 + 烧掉几块钱的 Token + 丢失半天进度**。

## 我们做了什么

**MagicLamp 项目迁移穿梭机** 是一套零 Token 消耗的确定性脚本，在交接发生前完成：

- **物理隔离**：把 AI 工具缓存、日志、编译产物从扫描范围中物理剔除
- **双语断点**：自动从 PRD/需求、README/CHANGELOG、TODO/任务中提取当前进度，统一归一至 `[DONE]` / `[ACTIVE_BREAKPOINT]` / `[IRON_LAW]`
- **真理源仲裁**：文件被改了？Git 提交时间 vs 暂存区 vs 物理时间三级判定，死锁时标记 `[CRITICAL_CONFUSED_ZONE]` 交给高性能模型裁决
- **凭证洗涤**：任何 API Key / Token / 密码 → `[REDACTED_SECRET_HASH_SHA256:...]`，保留字段名，只抹去值
- **缓存强固化**：交接书的静态头部逐字节稳定，云端提示词缓存命中后成本再降一个数量级

最终产出一份 `DEHYDRATED_CONTEXT.md`——一页纸，让新模型"读一页即满血续接"。

## 关键数字：省了多少 Token?

| 指标 | 数值 |
|---|---|
| **Token 节省率** | **94.7%**（现实场景平均） |
| **文件体积压缩** | **168 倍** |
| 盲启动成本 | $0.518 / 次 |
| 交接书成本 | $0.001 / 次 |

> 数据来源：4 档合成项目（tiny→large）代理估算 + 真实模型 A/B 双臂测试

## A/B 双臂实测：不只是省 Token，是真的快了

我们在一个 15 文件半成品 Node.js 项目上，用同一模型跑了 5 次盲启动（A臂）和 5 次带交接书启动（B臂）：

| 指标 | A 盲启动 | B 交接书 | 改善 |
|---|---|---|---|
| 到答轮数 | 4.6 ± 0.5 | 2.0 ± 0.6 | **-56.5%** |
| 工具调用次数 | 9.2 ± 1.2 | 2.8 ± 1.6 | **-69.6%** |
| 输入 Token | 4695 | 2326 | **-50.5%** |

注意：这还只是**小项目**。项目越大，盲启动反复试探的损耗越爆炸。

## 两种用法

**快照迁移（Snapshot）**：项目还没有交接书时，一键生成 `DEHYDRATED_CONTEXT.md`，提示用户切低成本模型拍快照。

**续接开发（Resume）**：项目已有交接书时，判定新鲜度与 git 漂移，直接恢复上下文进入开发。

```bash
# 零 Token 路由探测
python scripts/dehydrate.py --root . --detect

# 生成交接书
python scripts/dehydrate.py --root . --out DEHYDRATED_CONTEXT.md
```

## 技术细节

- **零依赖**：Python 3.8+ 系统自带，不装任何第三方包
- **零模型调用**：栈检测、文档检索、冲突仲裁、覆盖审计、签名抽取全部本地完成
- **覆盖率门控**：当文档未提及的真实源码文件超过 15%，自动注入 AST 签名地图（只发类名/函数名，不发源码体）
- **中英文双轨**：中文 PRD 里的"开发中"和英文 TODO 的 `in progress` 都会被捕获

开源地址：[github.com/ala2017/MagicLamp-AI-Studio](https://github.com/ala2017/MagicLamp-AI-Studio)

---
*如果你也经历过"换工具后先花半小时让 AI 理解项目"的痛苦，这个项目就是为你做的。*
