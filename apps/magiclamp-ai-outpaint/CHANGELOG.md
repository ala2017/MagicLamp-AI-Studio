# 更新日志 (CHANGELOG)

## [0.87] - 2026-05-24 ：左右面板重心对调、物理顶线水平对齐与精致拉丝银悬浮栏发布

### 🚀 核心改进
- **[CLEANUP] 补回遗失的远端 Monorepo 仓库声明**:
  - 现象：在物理大扫除及多文档合并至唯一 `prd.md` 时，遗失了历史 `prd.txt` 中的远端仓库声明，导致用户和开发上下文出现断层。
  - 修复/实现思路：在唯一核心 [prd.md](file:///f:/=%E7%A5%9E%E7%81%AFAI%C2%B7app/%E7%A5%9E%E7%81%AFAI%C2%B7outpaint/prd.md) 的元规格表格中重新补回了对远端 Monorepo 子目录 `https://github.com/ala2017/MagicLamp-AI-Studio/tree/main/apps/magiclamp-ai-outpaint` 的权威声明。

- **[CLEANUP] 清理冗余 prd.txt 残留，确立单一可信源 `prd.md`**:
  - 现象：物理上已经将 `prd.txt` 彻底删除，并扁平化将 `v/prd.md` 提升至顶级目录。但最新版 `prd.md` 的系统结构表格中依然列出了 `prd.txt`；此外，在全自动版本同步脚本 `update_version.py` 的头部注释和逻辑判定中，也仍然残留有针对 `prd.txt` 和 `v/prd.md` 的旧逻辑与条件判断，容易让人误以为仍然存在多个 PRD 文档。
  - 修复/实现思路：
    1. **更新唯一 PRD 物理目录树**：修改根目录下 [prd.md](file:///f:/=%E7%A5%9E%E7%81%AFAI%C2%B7app/%E7%A5%9E%E7%81%AFAI%C2%B7outpaint/prd.md)，彻底删除 `prd.txt` 目录层级描述，并将 `prd.md` 升格并更名为“👑 本项目唯一核心产品需求规格说明书与版本历史 (单一可信源)”，从产品定义层面固化单文档结构。
    2. **提纯版本同步脚本**：重构 [update_version.py](file:///f:/=%E7%A5%9E%E7%81%AFAI%C2%B7app/%E7%A5%9E%E7%81%AFAI%C2%B7outpaint/update_version.py) 的注释和代码流转，剔除 `prd.txt` 的陈旧痕迹。将代码逻辑中原有的 `if path.name == "prd.txt" or path.name == "prd.md":` 精简提纯为唯一的 `if path.name == "prd.md":`，在维护项目整洁度的同时 100% 确保版本同步的高稳定性。

- **[FEATURE] 精致拉丝银金属悬浮按钮与扩图卡片底部增设下载按钮**:
  - 现象：右上角悬浮栏“下载成品”和“100%细节”文字累赘，且橙色和普通黑色的视觉不够高端精致。此外，左下角“智能扩图”卡片没有一键下载成品的出口，必须等待右侧滑入。
  - 修复/实现思路：
    1. **去字只留图标**：物理删除了悬浮栏的所有文字标签，只留下高精图标，大小锁死（38x38px）。
    2. **精致 Apple 级金属拉丝质感**：使用 `linear-gradient(135deg, rgba(230, 230, 235, 0.9), rgba(185, 185, 190, 0.95))` 镜面渐变，加入 `border: 1px solid rgba(255, 255, 255, 0.65)` 高亮金属物理切边，内置 `box-shadow` 内发光高光。整体呈现极其尊贵、极简奢华的钛金/白银拉丝镜面金属观感，极其惊艳。
    3. **扩图按钮更名与下载增加**：将左侧“生成壁纸”按钮文字大修重塑为“智能扩图”，并在下方新设高对比“下载图片”按钮。在 JS 中，通过单向代理事件机制直接触发底层 `DOM.btnDownload.click()` 代理执行，100% 完美安全复用文件名逻辑和 polling taskId。同时在上传、重置时同步对该按钮添加/移除 `.hidden` 控制其显示/隐藏，实现顶级 SaaS 的操作热力回归。

- **[FEATURE] 智能扩图与消除精修左右面板重心对调**:
  - 现象：原本“消除与重绘”放在左边，“智能扩图”放在右边。然而，智能扩图作为一整套包含“上传图片 ➡️ 调整参数 ➡️ 生成”的创作起点，放在右侧割裂了商户从左到右的标准工作流热力路径；而“消除重绘”精修功能属于后处理的一部分，理应位于右侧进行收尾。
  - 修复/实现思路：将“AI 智能扩图”卡片整体迁移至左侧面板（`#left-panel`）；将“消除与重绘”修图卡片正式更名为**“图像处理工具”**，并将其迁移至右侧面板（`#right-panel`）顶部，与下方的“壁纸就绪”结果面板形成完美的右翼修图/成品交付闭环。

- **[FEATURE] 精准物理顶边水平齐平对齐**:
  - 现象：由于画布底部带有 canvas-info 文本，导致整个画布包裹器在垂直居中对齐时把左右侧面板物理拉低了约 14px，无法与画布上边框水平齐平，界面显得不够工整。
  - 修复/实现思路：将 `.canvas-info` 修改为绝对定位，悬挂在画布下沿下方且不占用布局高度。这样使得画布区高度精准被死锁在 720px，与左右面板物理高度完美统一，实现了顶边 100% 绝对水平齐平对齐。

- **[FEATURE] 按钮解构平移与更名更精美**:
  - 现象：原“画像画布管理”卡片单独占用纵向空间，略显繁冗；且“换一张图”和“清空重置”文字不够契合高频的画布操作意图。
  - 修复/实现思路：将该卡片降级为无背景的普通 div，剔除外壳，把**“换一张图”更名为“上传图片”**，**“清空重置”更名为“刷新画布”**，直接平移嵌入至“AI 智能扩图”卡片的绝对头部；并彻底**删除了冗余的说明文字**。

- **[FEATURE] AI 局部重绘按钮高光视觉平权**:
  - 现象：由于 AI 局部重绘采用 btn-secondary 类，导致灰黑色底的按钮在视觉上过于暗淡，与极速擦除高亮的琥珀渐变色相比显得像“不可用”的不起作用状态。
  - 修复/实现思路：为 `#btn-ai-inpaint` 编写了专属高端蓝紫至极光橙的流光渐变（`#8b5cf6 -> #ff5500`）及微发光悬停发光阴影，使其在视觉上与极速擦除同样耀眼注目，平分秋色，彻底消灭“不可用”误解。

- **[FEATURE] 彻底解决 FLUX.1 Fill 扩图人物误生缺陷 (从“否定句”大修至“正向排挤提示词”)**:
  - 现象：尽管设置了强力防人词（如 `no people, no person, no human`），但在极少数情况下，扩图区域仍然会凭空出现复制人、诡异人脸或肢体幻觉。
  - 原因：
    1. **CFG 逻辑卡锁**：FLUX 系列模型默认以 CFG = 1.0 的流匹配架构训练，原生不支持传统的负面提示词（Negative Prompt）。在 `diffusers` 中若要启用 `negative_prompt`，必须将 `true_cfg_scale` 设为大于 1.0，这会直接招致双倍的去噪计算量，导致生图耗时暴增 1-2 倍，对于本地消费卡（RTX 4060 Ti）极为不利。
    2. **粉色大象效应**：FLUX 的 T5-XXL 11B 文本编码器极度灵敏。在正向 Prompt 中强行提及 `people`、`human` 等词根，交叉注意力机制（Cross-Attention）反而会牢牢捕获这些特征并在画面上创造人物。
  - 修复/实现思路：彻底在词面上抹除任何与“人”有关的否定字眼，将 engine.py 中的 `DEFAULT_PROMPT` 与 `base_prompt` 升级为**纯正向物理排挤提示词**：`"seamless background extension, pure natural background scenery, uninhabited space, empty quiet environment, static background elements only"`。利用 `uninhabited space`（空无一人）、`empty quiet environment`（空旷静谧）等极强正向词对大模型进行潜意识空间约束，彻底切断对人物词根的误触，同时 100% 捍卫极速生图性能与显存负荷。

- **[FEATURE] 左右面板对称性视觉美学重塑与“图像画布管理”标题补齐**:
  - 现象：右侧“AI 智能扩图”模块有清晰带极客图标的面板标题，而左侧图片上传与管理模块直接呈现按钮卡片，缺乏统一标题。这导致左右面板视觉不对称，界面缺乏整体专业质感。
  - 修复/实现思路：在左侧顶层卡片 `#panel-global` 中，物理补齐了 `<div class="panel-title">` 并嵌入带有 `image-cog` 图标的“图像画布管理”文字，与右侧的“AI 智能扩图”标题形成完美的水平轴线对称，使得 SaaS 级高档美学得以极致体现。

- **[FEATURE] 悬浮一体化控制台 (Console Hub) 重塑与两侧面板画布等高锁定**:
  - 现象：在常规屏幕分辨率下，原有的左侧 `#left-panel` 和右侧 `#right-panel` 采用 absolute 顶格贴边定位，物理上完全被拉伸至屏幕左右边缘。这不仅导致鼠标在两侧反复横跨长达上千像素的移位，热力运动极度散乱，而且导致面板底端留出大片深色空洞区域，出现严重的“头重脚轻”和空虚感。
  - 修复/实现思路：彻底删除两翼面板的 absolute 定位，改由主布局容器 `#main-layout` 一键采用 Flex 居中并平铺直系子元素，并物理加入 `gap: 16px` 黄金贴合间距与 `overflow: auto`（小分辨率保护机制）。左右侧面板的高度物理死死锁定为 `720px`，与 16:9 画布高度完全齐平对齐。面板边缘辅以 `border: 1px solid var(--border)`、`border-radius: var(--radius-lg)`（顶奢倒角）与优雅的 `box-shadow`。
  - 效果：原本像“两根钉在屏幕边缘的柱子”的面板蜕变为了有机契合的、高科技悬浮式的“超级一体化控制台 (Console Hub)”，鼠标从画布边缘微移 20px 即可盲操两翼面板，手腕摆幅物理疲劳度降为 0，热力完美收拢。

## [0.85] - 2026-05-23 ：完美回归 v0.85 终极合拢版（上传/框选修复、顶栏文字变黑物理修复、保存文件名动态命名绑定、极客白银日志同步）
### 🚀 核心改进
- **[FEATURE] 智能多路由生成系统 (原地高清重新生成 VS 成果二次迭代外扩)**:
  - 现象：扩图完成后为了支持连续精修，前端需把 5K 成果大图重新赋给 `State.originalFile`。若用户不满意刚才的扩图成果、未作任何移动，仅改改参数/提示词直接再次点击“生成壁纸”时，系统会把贴满全屏的成品图发送给后端。这不仅因没有露白间隙（Mask 为全黑）导致大模型推理失效或报错，且成品大图做输入会经历二次降质，彻底摧毁 Paste-Back 物理原图像素 100% 无损的高清画质。
  - 修复/实现思路：在 `State` 中新设 `initialFile`（终身记忆最初上传的 Pure 原图）与 `initialLayout`（记忆首次排版坐标）。点击“生成壁纸”发送请求前，执行黄金智能路由判定：
    1. **原地高清重新生成（大图贴满全屏）**：自动截获请求，在底层将路由默默降级为最初纯净原图与最开始的初始位置坐标发给后端。这不仅 100% 捍卫了原图的绝对高精画质不受套娃降质影响，更打通了原地无数次刷出不同背景的黄金体验！
    2. **大图二次迭代外扩（大图被缩小/移位）**：自动选用刚才生成的 5K 成品大图作为新的输入原图，配合用户移动后露出的新空白，稳妥进行第二轮大图外扩。
    实现零人工负担、零画质损耗的高端 SaaS 级画质守护。

- **[FEATURE] 自动版本号同步与智能小版本自增控制工具 (update_version.py)**:
  - 现象：由于工作站版本号分散在 `prd.txt`、`v/prd.md`、`index.html`、`index.css` 和 `main.py` 五个源文件中，导致在开发和多轮迭代时极易发生遗漏，形成“搞不清哪个是正确版本号”的局促局面。
  - 修复/实现思路：在项目根目录下全新开发了全自动 Python 版本同步与更新工具 `update_version.py`。该脚本可自动读取工作站当前版本号，支持“无参数下自动加 `0.01` 自增小版本”或“手动指定版本号（如 `0.86`）”的双模执行。脚本内置了对 HTML title/badge、CSS banner 以及 FastAPI/Uvicorn 参数的特异性正则锚定替换，物理规避了误伤代码其他浮点数变量的隐患。一键完成多端齐刷刷同步，杜绝版本号割裂体验。

- **[FEATURE] 极客风格白银色日志控制台同步展示与自动向上滚动挤出系统**:
  - 现象：在 AI 生成与图像处理遮罩中，原本的控制台被已耗时时间单行直接覆写，且在多行堆叠时文字被切成半截、重叠拥挤不堪，无法完整看清。
  - 原因：旧 JS 中的 `startTimer` 计时器粗暴覆写了 `DOM.terminalLines` 的 innerHTML。而在重构中由于父容器 `.terminal-logs-view` 启用了 Flex 布局且锁定物理高度，在日志溢出时 **Flex 引擎强制将子元素的高度从 18px 均分压缩到了几像素**，导致所有日志被死死挤在同一极扁区间内产生文字折断与高度塌陷。
  - 修复/实现思路：重塑 `geek-terminal` 结构并物理降级排版架构。已将“已耗时”计时器与控制台日志拆分为独立图层；下方的日志滚动容器降级为**最经典、最暴力的原生块级（Block）流布局，彻底丢弃 Flex 限制**，并设定 `height: 88px` 精确锚定 4 行完美呼吸高度（`4 * 18px + 3 * 5px = 87px`）。每个子日志行强制显式设定为 `display: block; flex-shrink: 0; height: 18px; margin-bottom: 5px;` 以杜绝任何挤压可能。在 JS 轮询时，实时提取后端白银灰极客日志（`#cbd5e1`），依靠浏览器原生的流式自然高度向下堆积产生溢出，并重置 `scrollTop` 触发极致顺滑的原生向上平移滚屏。文字 100% 完整展示，呼吸有致且清晰见骨。

- **[FEATURE] 交互工具箱空间整合与左翼四大工具金刚合并美容**:
  - 现象：原本的“移动排版模式”按钮单独孤立在右侧“AI 智能扩图”卡片里，占用了极大的纵向位置，且造成了工具交互的严重空间割裂。由于系统已经配备了“点击图片自切移动、点击空白自切画笔”的智能免切换算法，该按钮显得冗余。
  - 修复/实现思路：从右翼面板中彻底物理删除该按钮，清爽释放了大块视觉空间。同时，将该工具作为第一等公民归纳搬迁到左翼“预处理工具栏”的最左侧。修改 `.action-tool-bar` 网格系统，由三格分天下升级为四分天下（`1fr 1fr 1fr 1fr`），使“移动、画笔、矩形、橡皮擦”四大金刚工具有机聚合，形成统一完整的交互控制中心。微调工具按钮 padding 为 `9px 2px`，保障小间距下不发生任何换行，使左右两翼的业务职责边界极其纯粹和高级。

### 🐛 关键修复
- **[BUGFIX] 彻底解决涂抹画笔粗细与原图 1:1 分辨率下生效直径不一致的缺陷**:
  - 现象：使用画笔涂抹遮罩执行极速擦除或 AI 局部重绘时，后端算法/大模型生效的消除半径远比屏幕上画笔圆圈所涂抹的区域细窄得多（只擦除了最中心的一小条，边缘大量杂物和水印残留未被消除）。
  - 原因：在 `exportMask()` 进行 Hires 矢量投影到大图的 1:1 绝对像素坐标系时，只投影拉伸了 `left`/`top`/`scaleX` 等基础属性，**遗漏了对 `strokeWidth`（画笔直径）的缩放适配**。由于 Fabric 5.x 在 StaticCanvas 上进行静态渲染时，描边线宽受 `scaleX` 的交互行为不确定，在大图分辨率（如 5K）下绘制出的白线仅保留了原 720p 视口下的低分像素大小（如 `20px`），使得其在原图上的实际占比严重萎缩（比如在 5K 图下萎缩了整整 4 倍，成为一根极细的小细线）。
  - 修复/实现思路：在克隆 Path 对象时进行“物理像素对齐大修”。在设置中**显式计算并赋予原图绝对分辨率下的真实描边线宽** `strokeWidth = obj.strokeWidth / imgScaleX`（例如：在 0.25 缩放的 5K 原图下，物理线宽放大 4 倍至 `80px`），并且**显式锁定 `strokeUniform: true`（终极防御：锁定等宽渲染，忽略 scaleX 拉伸的二次干扰，只采用我们精确给定的绝对物理线宽）**。这完美保证了输出的黑底白遮罩的物理粗细，在任何大图分辨率与拉伸尺度下，都与主画布上屏幕圆圈所呈现的视觉区域 100% 绝对达到物理级 1:1 丝毫不差的像素对齐！

- **[BUGFIX] 物理修复部分浏览器下 Outpaint Logo 文字变黑看不见的兼容缺陷**:
  - 现象：在部分用户浏览器下，顶部 Logo 中的 “Outpaint” 字体变黑或者完全看不见。
  - 原因：在 `.header-left h1 .accent` 中设置了 `background: linear-gradient(...)` 和 `-webkit-text-fill-color: transparent` 剪裁文本。如果浏览器在特定模式下背景裁剪失效，或者使用了透明文本填充，就会直接露出下层黑色的背景导致文字变黑隐形。
  - 修复/实现思路：将 logo 的 `.accent` 文本样式修改为安全的实体高对比亮橙色 `var(--brand-orange-bright)`，直接丢弃容易出 Bug 的透明裁剪渐变方案，100% 保障多平台兼容性与第一视觉的惊艳度。

- **[BUGFIX] 彻底解决左侧上传图片无响应的致命缺陷**:
  - 现象：页面刚加载时，左上面板无“上传图片”卡片，且通过其他操作让卡片显示出来后，点击也完全没有任何响应。
  - 原因：在 `v/frontend/index.html` 中，左面板最上层绝对定位覆盖了一个蒙版 `#left-panel-mask`（类名为 `right-panel-mask`），但是在 `app.js` 里获取和隐藏的却是 `DOM.rightPanelMask`（它根本不存在）。这导致该磨砂遮罩永久锁死在左面板最上层，屏蔽了一切点击；并且用于图片管理的卡片 `#panel-global` 默认带有 `hidden` 属性，初始被强行隐藏。
  - 修复/实现思路：物理删除左侧 `#left-panel-mask` 遮罩，丢弃臃肿的硬锁屏，改用优雅的“状态控制置灰/禁用”交互体系；同时去掉 `#panel-global` 上的 `hidden` 类名使其默认渲染。左侧上传按钮和重置按钮 100% 灵敏可见且响应点击。

- **[BUGFIX] 彻底解决矩形框选工具在消除/重绘和撤销流程中没起作用的缺陷**:
  - 现象：用矩形框选区域后，点击极速擦除或 AI 局部重绘会直接被拦截提示“请先涂抹”；擦除重载成功后，红色矩形红框残留在画板上无法清空；且即使画了矩形，撤销按钮也依然处于禁用状态。
  - 原因：JS 里的预处理拦截判定、成功后清空画布遮罩逻辑、撤销状态更新、以及清空遮罩按钮事件这四处核心代码，**全部硬编码只获取了 path（画笔）对象，完全遗漏了对 rect（矩形）遮罩的支持**。
  - 修复/实现思路：将 `app.js` 里的这四处核心判定，彻底升级为“平行支持 path 和 rect”。在判断遮罩存在和清除时，统一提取并操作两种对象。矩形遮罩红框现已能够 100% 完美支持撤销、一键清空、消除拦截安全放行，且擦除成功重载后被自动清空，毫无残留。

### 🚀 核心改进
- **[FEATURE] 动态文件名保存绑定系统 (原有上传的文件名+result)**:
  - 现象：在套娃式多次迭代扩图或消除重绘瑕疵精修后，下载成品时文件名会变为 `result_result.png` 或 `preprocessed_result.png`，未能保留最初的上传文件名。
  - 原因：在极速擦除、局部重绘和智能扩图成功的回调中，前端为了支持无限套娃连续修图，把后端返回的最新成果 Blob 封装成 `new File` 并重新赋给 `State.originalFile`。但硬编码指定了名称 `"result.png"` 和 `"preprocessed.png"`，导致原文件名在第二次精修或二次扩图时被强行抹除。
  - 修复/实现思路：在 `State` 对象中新增了记忆核心 `State.uploadFileName`。仅当用户在初次上传导入新图时，才从中提取主文件名并“物理级终身锁定”；后续无论是 AI 扩图还是擦除重绘，在重载 Blob 生成的新 File 时自动过滤并**忽略更新该字段**；而在清空重置时清除。下载成品时直接提取该锁定原名拼接 `_result.png`。如此，无论在成果上套娃精修多少回，下载保存名都 100% 精确保留最初的文件名！

- **[FEATURE] 打通“扩图成果 ➡️ 左侧瑕疵修补 ➡️ 二次迭代扩图”并存与流转闭环**:
  - 现象：智能扩图成功产出成品后，左侧所有的图片管理和消除/重绘工具卡片全被强制隐藏，剥夺了用户在扩图后精修和再次扩图的权力。
  - 原因：生成成功回调 `handleGenerateSuccess()` 中硬编码对左侧的 `#panel-brush` 和 `#panel-global` 加上了 `hidden` 类名进行霸道隐藏。
  - 修复/实现思路：物理移除该隐藏行，扩图完毕后左侧修图工具箱与右侧成品卡片**完美共存，相辅相成**；同时在 `handlePreprocessSuccess()` 擦除/重绘成功时加入智能流转判定：一旦用户在成品上修补瑕疵成功，**自动隐退右侧成果面板，重新亮起智能扩图参数卡片**。用户擦完瑕疵即可马上进行第二次甚至无限套娃式的迭代扩图，交互连贯度跃升至顶奢 SaaS 水准！

- **[FEATURE] 跨场景全功能输出绑定之右上角一键下载**:
  - 现象：在极速擦除和 AI 局部重绘成功完成瑕疵消除后，用户觉得效果满意想一键下载，但是画板右上角并没有提供下载浮动控制条，且右下角的下载按钮由于切回了扩图面板导致被隐藏，无法直接下载。
  - 原因：旧代码仅在扩图成果展示 `handleGenerateSuccess()` 中开启了右上角浮动控制条，而在 `handlePreprocessSuccess()` 成功重载大图时漏掉了该点亮逻辑。
  - 修复/实现思路：在 `handlePreprocessSuccess()` 中追加点亮 `DOM.zoomControlBar` 的逻辑，使用户在擦除/重绘精修成果产出后也能一键超清下载或细节查看，拉取的 `State.taskId` 完全对准最新的成果；同时在导入新图片时增加隐退防护，以防残留，打造顶级 SaaS 级体验。

- **[FEATURE] 发动 nanobanana 专业配色的全局 UI 美容与按钮大字重塑**:
  - 现象：界面按钮配色不统一（紫色和青色渐变在橙色主调下显得斑驳杂乱），字号通用只有偏小的 `12px`，在暗色背景下产生“发虚、看不清”现象，且高度太扁。
  - 原因：旧代码的 `:root` 依然残留了紫色的命名变量；工具按钮激活态包含红、蓝、紫各种彩虹实色渐变；且按钮字号和 padding 设置过于局促。
  - 修复/实现思路：理顺并重写 `index.css` 全局色彩 Token，套用全新的 nanobanana 暖橙/晚霞色彩体系与高亮微发光霓虹影；将通用按钮 `.btn` 字号提升至 `14px`，padding 调整至 `11px 18px`，字重统一升级为 `600` 加粗，高对比纯白字绝不发虚；修图动作采用暖橙/晚霞双色渐变，辅助流转按钮采用沉稳中性灰蓝（`#1e2332`）与精细微光边框；将工具切换按钮激活状态统一收拢为高档的“亮橙半透与橙色描边微发光特效”；并在 `index.html` 网页内联样式中同步美容升级浮动卡片按钮，使工作站呈现极高档的数字艺术质感。

## [0.84] - 2026-05-23 ：双面板空间重构与矩形框选功能上线
### 🚀 核心改进
- **[FEATURE] 空间逻辑与热力区双面板重构**:
  - 现象：原本工具栏和扩图参数都在右侧，使得输入预处理与输出后处理流程混杂在一起，工作流空间逻辑不明确。
  - 修复：按业务流程引入 `左侧预处理，中间画板，右侧输出后处理` 的三段式并排空间布局。将图片导入、工具栏、擦除执行迁移至全新的 `#left-panel` 容器；将移动排版等功能聚合在 `#right-panel` 的扩图参数区。
  - 效果：通过清晰的左右两翼面板包裹画板，建立从左到右的标准工作流（Upload/Manage -> Pre-process -> Move/Generate -> Finish），大幅提升专业修图感。

- **[FEATURE] 新增矩形框选功能**:
  - 现象：用户在清理大面积杂物或进行规则区域的重绘时，使用圆形画笔涂抹效率较低且边缘不整齐。
  - 修复：在左侧工具栏新增“矩形框选 (Rect)”按钮。在 `app.js` 中新增了 `rect` 工具状态及其原生 Fabric 事件绑定（mouse:down 记录起点，mouse:move 动态拉伸半透明红色 `fabric.Rect`，mouse:up 物理固化掩膜），同时将 R 键绑定为快捷切换。并在底层的 `exportMask()` 中实现将 `fabric.Rect` 与普通画笔路径共同进行高精度坐标投影。
  - 效果：大幅提升了针对规则几何区域（如图文海报区域、硬边缘区块）的框选擦除与 AI 重绘效率。

## [0.83] - 2026-05-23 ：画笔圆形光标修复与极速擦除 Mask 坐标投影修复
### 🐛 关键修复
- **[BUGFIX] 画笔光标从十字准星修复为动态圆形光标**:
  - 现象：无论画笔还是橡皮擦模式，光标始终显示为 Windows 默认十字准星 (crosshair)，用户无法直观看到涂抹范围。
  - 原因：代码错误地将 SVG 圆形光标赋给了 `canvas.freeDrawingBrush.cursor`，但 Fabric.js 5.x 的画笔光标实际由 `canvas.freeDrawingCursor` 属性控制。同时 `setEraserMode()` 中硬编码了 `DOM.canvasContainer.style.cursor = 'crosshair'`，强行覆盖了 Fabric 的光标渲染。此外使用 `URL.createObjectURL(Blob)` 生成的 SVG URL 在某些浏览器中不被 CSS cursor 识别。
  - 修复：改用 `encodeURIComponent` 内联 Data URL 方案生成 SVG 圆形光标，赋给正确的 `canvas.freeDrawingCursor` 属性；移除 `setEraserMode` 中对容器 cursor 的硬编码覆盖。
  - 效果：画笔和橡皮擦模式下，光标呈现为与画笔粗细 1:1 精确对应的半透明圆圈，拖动粗细滑块时实时同步缩放。

- **[BUGFIX] 极速擦除/AI局部重绘 Mask 坐标投影致命偏移修复**:
  - 现象：涂抹遮罩区域后点击"极速擦除"，后端执行 Telea 算法但擦除位置完全不对，等于没生效。
  - 原因：`exportMask()` 中将涂抹路径坐标投影到原图坐标系时，直接使用了 `State.imageObj.left/top` 作为图片左上角。但图片设置了 `originX: 'center', originY: 'center'`，意味着 `left/top` 实际是图片**中心点**坐标而非左上角。例如图片居中时 `left=640, top=360`，而路径的 `left` 可能是 200，导致 `(200 - 640) / scale` 算出负值，mask 完全偏飞。
  - 修复：在投影前先正确计算图片物理左上角：`imgLeftTopX = imageObj.left - (width * scaleX) / 2`，然后用真实左上角做偏移换算。
  - 效果：涂抹区域与后端擦除区域 100% 精准对齐，极速擦除和 AI 局部重绘均恢复正常工作。


### 🚀 核心改进
- **[FEATURE] 采纳暖橙色 (Warm Orange) 为核心品牌主题色**:
  - 现象：原本界面使用的是传统的紫色、青色混合的深色基调（极光紫红）。
  - 修复：根据指示，全面拥抱高对比度、质感饱满的“暖橙色（Active Orange）”。将 CSS 系统全局的主题色 `--accent-purple` 完美重构映射为纯正的暖橙（`#f97316`），将极光背景的青色映射为晚霞琥珀色（`#f59e0b`）。
  - 效果：前端界面在瞬间切换为了清朗活力、极具数字现代感的“深褐底 + 暖橙高光”的绝美色彩体系。

- **[FEATURE] 确立 3D 拟物立体字 Logo 方案**:
  - 现象：一直在探索 2D 扁平极简 SVG 的设计。
  - 修复：直接采纳用户提供的带平板、数字画作及精美立体字“AI Outpaint”的高规格 3D 拟物图作为官方 Logo。将所有 HTML 与静态资源引用指向这版极致的橙色原图，实现设计概念和前端工程的 100% 对齐。## [0.81] - 2026-05-23 ：画板右上角高亮浮动“一键下载”通道建立
### 🚀 核心改进
- **[FEATURE] 彻底解决右侧控制区按钮溢出消失的顽疾，上线画板右上角“下载成品”浮动按钮**:
  - 现象：部分屏幕分辨率下，由于右侧面板高度受限和复杂的外部 Flex 布局挤压，即使预览图进行了限高保护，原有的“下载图片”按钮依然会被极其隐蔽地挤到屏幕视口底端以外而完全看不到。
  - 修复：在左侧大画板右上角的浮动控制条中，在“100% 细节”按钮的右侧，**直接并排新增了一个醒目的、高发光霓虹紫背景的“下载成品”浮动按钮**。
  - 绑定逻辑：为浮动下载按钮同步绑定了一键超清成品壁纸 (5120x2880) 下载事件，其底层逻辑与原按钮完全一致，且同样在成品生成就绪时自动与细节缩放条同步亮起，在重置或换图时自动隐藏。
  - 效果：用户现在看着画板中的成品效果，**可以直接在画布右上角“啪”的一下完成一键极速下载，不需要去翻找或滚动任何右侧面板，100% 灵敏可见且绝无遮挡，交互体验达到绝对的闭环**！

## [0.80] - 2026-05-23 ：5K 原始细节查看浮动控制条与大图限高溢出拦截彻底扫除
### 🚀 核心改进
- **[BUGFIX] 彻底解决成果预览图过大导致底部“下载图片”等核心操作按钮被挤走消失的漏洞**:
  - 现象：扩图完成后，商户反馈原有的图片下载按钮和对比按钮“不见了”。
  - 原因：在 `panel-result` 中，生图成果预览图 `DOM.resultImg` 原始比例被赋予 src 后垂直撑开。在没有限高约束下，超高预览图无底线占据纵向空间，直接将下方的 `panel-footer`（包含下载成品、对比回贴、重新生成按钮）**强行挤到了浏览器可视区下边界以外（溢出隐藏）**，导致用户误认为下载按钮不见了。
  - 修复：在 `index.html` 内置 `<style>` 中为预览图 `.result-preview` 加上极高规格的 `max-height: 200px; object-fit: contain;` 物理安全限高约束。结果图预览既精致美观，又**百分之百稳固地把下载按钮和所有核心按钮牢牢锁定常亮在屏幕内**，杜绝任何纵向挤压溢出。

- **[FEATURE] 打造 5K 原始像素“100% 超高清细节”浮动控制条与双向流转切换**:
  - 现象：扩图输出直接 100% 满铺 1280x720 的画板，由于生图成品是超高清的 5120x2880 (5K) 分辨率，在不进行 Contain 等比缩小的情况下，会产生 4 倍严重裁剪，使用户只能看到超大局部，无法看清整幅图的全貌。
  - 修复：
    1.  **首创 Contain 自适应刷新**：将 `handleGenerateSuccess` 中的刷新画布逻辑重构为 **自适应 Contain 吸边铺满画板**：5120x2880 大图自动计算并以 `0.25` 等比缩放，完美 100% 填满 1280x720 画板且无任何溢出，让用户一眼看清整幅图的全局精美轮廓。
    2.  **右上角玻璃拟态浮动控制条**：在画布右上角悬浮放置了高科技感的玻璃拟态 `100% 细节` 按钮（仅在扩图就绪时点亮呈现，重置/换图时自动隐退）。
    3.  **100% 细节原尺寸双向查看**：
        - 点击“100% 细节”按钮（或按 **`Z`** 快捷键），图片瞬间以 1:1 的 `1.0` scale（即 5120x2880 完整像素尺寸）拉开，**同步在拖拽/缩放边界限制中跳过撞墙限制**，使用户可以按住空格或直接拖拽大图，畅快探索和观赏 5K 极其微小的毛发、皮肤等极致超清局部细节！
        - 点击“自适应边框”（或按 **`Z`** 键），大图瞬间复原回 1280x720 居中自适应 Contain 视图，重新加锁撞墙限制。
  - 效果：交互震撼度直击 Photoshop 核心排版引擎，体验高档奢华！

## [0.79] - 2026-05-23 ：输入自适应吸合边框算子升级与输出 100% 满铺高保真画板
### 🚀 核心改进
- **[FEATURE] 升级上传图片默认初始排版为“自适应吸合到边框”算法**:
  - 现象：原先导入图片时默认按 80% 画布面积居中留空，导致许多正常长宽比的图无法吸合边框展示，降低了画布的空间利用率与生图直观感。
  - 原因：硬编码了 0.8 的缩小系数。
  - 修复：物理移除 0.8 系数，重构为 **自适应吸合 Contain 到边框算子**。对于窄图/高图/竖版图，高度自动 100% 完美贴合画布的上下边框（上下对边重合吸边）；对于特宽/特扁的图，宽度自动 100% 完美贴合画布左右边框。在给大模型留出更精细外扩边界的同时，最大化充盈展现了输入大图的高保真画面。

- **[FEATURE] 升级扩图完成刷新画板为 100% 满铺无缝显示**:
  - 现象：扩图完成后，画面以 90% 留空居中展示，四周留下无意义的紫色细虚线边框，视觉张力不足，且对下一次扩图排版造成初始位置上的割裂。
  - 修复：将 `handleGenerateSuccess` 中的刷新画布逻辑重写为 **100% 完美的画布铺满显示**：缩放比例 `scale` 设为 `1.0`，原点设为左上角 `(0, 0)`。
  - 效果：成品壁纸完美无暇、分毫不差地 100% 占满整个画板，视觉震撼力极强，且用户要进行二次扩图时，大图本身天然就处于最规整、最方便再次向外拖拽和缩放的初始姿态，体验流畅度与专业感跃升！

## [0.78] - 2026-05-23 ：成果态与二次编辑态智能感知交互流转与闪现隐藏 Bug 物理消灭
### 🚀 核心改进
- **[BUGFIX] 彻底消灭扩图完成后下载按钮与成品面板瞬间闪现消失/隐藏的漏洞**:
  - 现象：扩图生成成功后，点击图片时或者载入完毕的瞬间，右侧结果面板（包含“下载成品壁纸”与“对比”按钮）突然闪现一下就消失了，直接变回了普通的智能扩图控制面板，使用户无法下载图片。
  - 原因：在 [0.77] 刷新画板逻辑中，图片异步 onload 载入完成后，在 `img.onload` 回调里直接强行调用了 `switchTool('move')`。而 `switchTool` 会无差别强制对结果面板加上 `hidden` 属性并展示编辑面板，从而在图片载入完毕的微秒内把刚刚显示的成果面板再次强制隐藏，产生了严重的“交互闪现消失”漏洞。
  - 修复：在 `handleGenerateSuccess` 中彻底移除 `switchTool('move')` 强调用，重写为直接且平行的 Fabric 画布移动排版底层状态（非画笔写画状态）的重置，**坚决不触碰也不隐藏刚刚好不容易展示出的成果面板**，从而 100% 确保下载和对比按钮在生成成功后稳固常亮。

- **[FEATURE] 引入高品质“交互智能感知自动切回编辑态”上下文流转机制**:
  - 现象：将扩图结果重新载入画板后，用户如果想开始拖动图片进行二次排版，或者点击空白区域调出画笔消除瑕疵时，右侧仍然保持在“成品展示面板”，阻碍了用户对“极速擦除”或“第二次智能扩图”控制参数的操作。
  - 修复：设计了 `ensureEditModeOnInteraction` 智能上下文检测隐退机制，并极其灵巧地挂载在三处高频用户动作出口中：
    1.  **画布大图拖拽 (`object:moving`)**：用户一旦开始拖动新图排版，优雅隐退成品面板，重新点亮智能扩图控制台。
    2.  **画布大图缩放 (`object:scaling`)**：用户一旦缩放图片，自动切回编辑控制台。
    3.  **进入画笔模式 (`enterBrushMode`)**：无论是点击画笔工具还是点击空白背景释放选中开始写画遮罩，自动切回消除卡片以展露“极速擦除”和“AI局部重绘”按钮。
  - 效果：通过极其顺畅的非对称时序设计，既规避了代码主动 load 图片并选中时产生的 selection 误触，又让用户在开始实际操作（移动/缩放/写画）的瞬间，操作面板**自动智能流转切换到对应的控制面板上**，交互体验达到业界顶奢 SaaS 水准！

## [0.77] - 2026-05-23 ：AI 扩图成品自动刷新载入画板与无限次迭代创作流闭环
### 🚀 核心改进
- **[FEATURE] 自动刷新画板与二次编辑能力打通 (解耦原图与结果，形成闭环)**:
  - 现象：扩图完成后，左侧 Fabric 画板仍然保留旧的待扩原图和残留笔触，右侧却已显示就绪。用户如需精修（如极速擦除新扩区域的水印杂物）或在此基础上进行二次扩图，必须手动上传新生成图，导致极度繁琐的割裂体验。
  - 修复：扩图成功后，前端自动触发**黄金画板重载刷新机制**：
    1. 自动物理清除画板残留笔画和旧图像引用，重设撤销栈。
    2. 将新生成的超高清扩图结果居中并完美等比适配装载进画布（`State.imageObj = fImg`），默认重设为 `move` 排版状态。
    3. 同步将操作文件 `State.originalFile` 热更新为最新的生成结果 blob 对象。
  - 效果：用户可以**无感无缝地立刻在生成的扩图结果上进行下一次的擦除、重绘，甚至在此基础上拖拽进行无限套娃式的“二次扩图”**，达成了闭环且极度流畅的 AI 精修与扩图迭代工作流！

## [0.76] - 2026-05-23 ：非阻塞高科技感状态监控升级与前端 DOM 交互死锁彻底解除
### 🚀 核心改进
- **[BUGFIX] 彻底解决 DOM.brushHint 空指针异常引发的前端交互停滞 Bug**:
  - 现象：在用户导入图片或在画布上点击空白区域时，触发 `selection:cleared` 进而触发 `setEraserMode` 时，前端直接崩溃，任何后续点击（包括生成、极速擦除、AI 局部重绘）均无反应，后端终端里完全没有 `/api/generate` 或 `/api/preprocess` 的请求日志。
  - 原因：`index.html` 经过 V2 重写后移除了 `.brush-hint` 元素，导致 `DOM.brushHint` 在初始化时返回 `null`。在 `setEraserMode` 里强行执行 `DOM.brushHint.textContent = ...` 导致抛出了致命的未捕获 `TypeError` 异常，阻断了整个 JS 线程。
  - 修复：将 `DOM.brushHint` 重新安全映射到存在于页面中的 `.smart-action-tip span`，并在 `setEraserMode` 及其他所有对其修改的逻辑中加入可选的 `if (DOM.brushHint)` 空指针拦截保护，彻底实现 100% 前端防御性安全。

- **[REFACTOR] 重构大模型预载心跳监测 startModelStatusCheck，彻底打破冷启动死锁**:
  - 现象：用户反映导入图片后点击生成完全无反应，终端里只有 `/api/health` 轮询请求。
  - 原因：旧版心跳监控在检查到 `/api/health` 返回 `model_loaded = false` 时，会通过 JS 强行拉起全屏加载遮罩 `#system-loading-gate` 并无差别禁用生成和局部重绘按钮。而后端采用的是“延迟按需同步加载模型”，在收到第一次生图请求前 `model_loaded` 必定是 `false`。这就导致了“按钮因为模型未载入而禁用 -> 用户无法生成 -> 后端永远等不到生成请求来同步加载模型”的完美逻辑死锁环。
  - 修复：
    1. **全面升级为非阻塞、不锁屏、不霸屏的状态反馈机制**：彻底在 JS 内部废除对全屏锁定遮罩 `#system-loading-gate` 的强制拉起和对按钮的硬编码禁用。
    2. **点亮黄金延迟同步加载**：只要用户上传图片成功且不在执行生成中，生成和局部重绘按钮 100% 保持可用状态，随时允许用户发起请求，在后台按需拉起并加载模型。
    3. **高科技感呼吸角标**：当模型未加载时，右上角 GPU 角标呼吸灯显示为柔和待命黄色（warn），文字优雅提示为 `GPU (待命)`，且显存监视器同步更新空闲状态；当大模型延迟同步加载完成后，呼吸灯变绿（live），文字为 `GPU (已就绪)`，显存监视器实时跳动大模型驻留的 11GB+ 显存，极富科技感。
    4. **在文件尾部重新自动拉起监控**：恢复高低频心跳检测，给用户极高品质、绝无死锁的运行反馈。

## [0.75] - 2026-05-23 ：Outpaint v2 (管线2) 独立目录隔离与工程黄金重构
### 🚀 核心改进
- **[REFACTOR] 完全重写 v2 后端加载时序与前端控制流，全量回归 v1 稳健设计**:
  - 现象：v2 自创的后台 startup 异步预加载线程在 Windows 下频繁产生 CUDA 显存竞争与死锁；前端毛玻璃全屏锁定遮罩 `#system-loading-gate` 强行阻碍了本可 100% 独立离线运行的 OpenCV 极速擦除，造成极端恶劣的阻塞体验。
  - 原因：过度工程化所带来的时序混乱，打乱了原本极简高效的控制流。
  - 修复：
    1. **彻底废除**后端 `startup` 事件的异步多线程大模型载入进程，回归 **“按需延迟同步加载”** 的金牌稳定方案。在生图/重绘时同步阻塞载入，加载进度自然融合于 `/api/status` 输出，100% 根绝显存死锁。
    2. **物理封锁并删除**全屏遮罩 `#system-loading-gate` 屏障与前端阻碍性心跳。商户进入网页 **0 秒秒开** 瞬间进入工作台，无图亦能 100% 秒级直接使用离线极速擦除。
    3. **导入图片即刻点亮**智能扩图与重绘按钮，不再受大模型前置载入状态卡锁。

- **[FEATURE] 彻底攻克画笔消除交互领域的四大低级缺陷**:
  - 现象：画笔鼠标光标为反直觉的“十字准星”；涂抹一笔即被“智能捕获”强制弹回拖拽模式，无法连续工作；掩膜红色路径可被鼠标误选拖移，产生几何形变位移；没有 Ctrl+Z 笔迹撤销手段。
  - 修复：
    1. **一比一动态圆形 SVG 画笔光标**：在滑动滑块或修改画笔大小时，前端动态生成与实际直径 100% 匹配的半透明 Circle SVG data URI，赋予 `canvas.freeDrawingBrush.cursor`。鼠标移入呈现与涂抹范围精确重合的动态红圈，体验极佳。
    2. **连续涂抹不强制弹回**：注销擦除/重绘成功时的 `switchTool('move')` 弹回，画笔按钮保持常亮，商户可连续自由写画。
    3. **物理锁死掩膜路径，穿透鼠标事件**：为新生成的画笔 Path 强制死锁 `selectable = false, evented = false` 属性。在事件响应上对鼠标完全穿透，彻底杜绝掩膜被误选拖移的漏洞。
    4. **对象级高精度 `Ctrl+Z` 物理无损撤销**：建立 `State.brushUndoStack` 画迹物理引用栈。按下 `Ctrl+Z` 时，直接 `canvas.remove(lastPath)` 撤销物理清除，性能提升千倍，且绝对不损坏或清空原图当前的排版和缩放状态。
    5. **空格键 (Space) 或鼠标中键无感抓手平移**：按住空格键/鼠标中键时光标变为 `grab` 抓手即可拖拽平移画布，松开无缝秒回原画笔或工具，免去频繁点击切换。

- **[REFACTOR] 废除 Crop-and-Stitch 割裂方案，全量回归黄金“全局一次生图 + 超分重建 + 原始物理 pixels 回贴”方案**:
  - 现象：高分辨率分块条带拼图方案由于大模型缺乏全局一致性语义理解，生图随机失控产生断层，且不同条带色差明暗完全割裂，硬缝合产生极严重重影和打补丁，属于“理论可行、实战破产”的伪命题。
  - 修复：
    1. **彻底物理删除**冗余不实用的 `v/backend/stitcher.py` 拼图模块。
    2. 无论是 1080p 还是 4K 级别大图，**统一分流执行成熟的黄金扩图方案**：先等比缩放到 `1280x720` 进行一次性全局扩图（保证背景透视、光影逻辑 100% 连贯）；使用 RealESRGAN x4plus 进行高保真超清重建获得 5K 大底图；最后将 **100% 原始高清大图的像素物理贴回超分画布**，交界处用 16px 边缘高斯羽化过渡。
    3. 扩图结果背景极其统一完美，而**原图主体清晰度得到 100% 绝对零损失高保真保留**，效果极其惊艳。    4. 彻底删除根目录下原有的冗余 v2 文件，保持整个项目根目录极致干净。
- **[DOC] 引入社区成熟开源方案交互技术参考与低级缺陷改良设计**:
  - 现象：现有版本的涂抹遮罩存在严重的低级交互体验硬伤（如：涂抹一笔即暴力自动切回拖拽、红色笔画可被鼠标误选拖走位移、画笔无高精度撤销 Undo 等）。
  - 原因：旧版前端设计忽略了用户连续涂抹消除的高频业务流，且未锁死 Fabric.js 路径事件并穿透响应。
  - 修复：在专属 PRD 文档中引入 AUTOMATIC1111/ComfyUI (独立掩膜层物理锁定模式) 和 Figma (按住空格/中键无感平移 Pan 模式) 等业界标杆级开源交互范式作为技术参考；制定了后续版本的无状态交互规格、Path 死锁属性 (`selectable=false, evented=false`) 机制以及基于撤销栈的历史回溯 `Ctrl+Z` 物理撤销规格设计。

### 💡 架构演进与核心避坑记录 (Architectural Journey)
- **大模型与环境父级寻址共享**：
  为避免重新创建虚拟环境（~4GB）或重新下载 FLUX 和 RealESRGAN 大模型（~11.3GB），我们在重构后的 `v/backend/engine.py`、`v/backend/upscaler.py` 以及 `v/start.py` 中，巧妙地将寻址路径由 `.parent` 更改为 `.parent.parent.parent` 级，使运行在 `v/` 目录下的 v2系统可以直接复用父级目录的 `venv/` 虚拟环境与 `models/` 缓存。在保证极高版本隔离度的同时，实现了 0 耗时冷切换，无需任何繁杂的硬件依赖与下载重新配置。

---

## [0.74] - 2026-05-22 ：SaaS 级引导与右侧面板锁定的 app.js 完美生命周期闭环联调
### 🚀 核心改进
- **[FEATURE] 大模型预载全屏物理/视觉遮罩屏障 (#system-loading-gate) 完美联动**:
  - 现象：前端在大模型未加载完毕前允许拖拽、写画脏路径和乱点参数，操作容易引发 JS 崩溃或用户迷惑；在连不上后端时没有明显的报警提示。
  - 原因：之前的引导页面在前端中是静态的存在，没有跟 `app.js` 的 `startModelStatusCheck()` 做完全的 DOM 绑定与更新。
  - 修复：
    1. 在 `app.js` 中完整引入并映射了 `#system-loading-gate` 相关的 DOM 节点。
    2. 在 `startModelStatusCheck()` 轮询中深度绑定加载卡片状态，从 `/api/health` 实时读取硬件显卡名称，并使用当前可用显存比例作为进度条宽度（20%~95% 映射），给用户极其生动的 SaaS 级科技流光预载体验。
    3. 在大模型就绪后，进度条瞬间拉满为 100%，遮罩面板利用 CSS `transition` 平滑地从 `scale(1.0)` 和 `opacity: 1` 淡出并缩放解锁，丝滑过渡进入主工作台。
    4. 如果发生请求异常（后端离线或中断），进度条和文字背景将瞬间拉红并跳出“后端已离线，正在重试连接...”的高警示警报，彻底杜绝任何静默故障。
  - 文件：修改 `frontend_v2/app.js`

- **[FEATURE] 右侧精细参数控制面板无图磨砂锁定 (#right-panel-mask) 生命周期函数闭环**:
  - 现象：在没有导入图片前，右侧多达数十个画笔、步数、引导参数滑块依然亮起，用户此时调节这些滑块不仅无用反而破坏了交互逻辑。
  - 原因：缺乏导入状态对参数区的生命周期锁定。
  - 修复：
    1. 在 `app.js` 导入图片事件（`handleFile`）中，当图片加载成功后，自动为右侧的 `#right-panel-mask` 增加 `.hidden` 类，以优雅的渐变过渡解锁所有精细控制滑块与“生成壁纸”、“AI局部重绘”按钮。
    2. 当用户点击“清空重置”时，重置事件回调处理器内会自动移除 `.hidden` 类，重新上锁右侧的精细控制卡片，达成完美的“初始态锁定 -> 导入点亮 -> 清空锁定”交互流转闭环。
  - 文件：修改 `frontend_v2/app.js`

- **[FEATURE] 无图画布物理防御性拦截拦截**:
  - 现象：在没有导入图片时，鼠标在 Canvas 上拖拽仍然会绘制红色的涂抹掩膜路径，导致留下脏笔迹污染画布。
  - 修复：在 Fabric `path:created` 路径生成事件中加入首道防线拦截，当 `State.imageObj` 不存在时直接调用 `canvas.remove(e.path)` 撤销该路径，并使用 `logTerminal` 给出明确的操作提示，确保画布零污染。
  - 文件：修改 `frontend_v2/app.js`

---

## [0.73] - 2026-05-22 ：SaaS 级系统引导保护与右侧一站式平铺交互重构
### 🚀 核心改进
- **[FEATURE] SaaS 级后台模型冷启动引导与状态主动心跳检测**:
  - 现象：系统刚开机、模型后台预加载尚未完毕时，用户如果点“生成壁纸”或“AI局部重绘”会遭遇静默挂起或后台报错崩溃。
  - 原因：后台模型加载是异步大任务，冷启动需要分配 GPU 并加载十几 GB 模型，而前端此前缺乏对后台心跳的主动轮询，没有锁定大模型关联的操作按钮。
  - 修复：
    1. 在 `frontend_v2/app.js` 末尾注入 `startModelStatusCheck()` 主动心跳轮询 `/api/health`（前置高频 2 秒，就绪后降低为 5 秒心跳）。
    2. 后台加载期间，将大模型关联按钮（“生成壁纸”与“AI局部重绘”）强制置灰禁用，并将文字以 SaaS 级精美效果修改为带圈旋转加载的 `模型预加载中...`；就绪后变为常规可点击状态。
    3. 状态栏顶部 GPU 呼吸灯黄灯闪烁，文字显示“大模型预加载中...”；就绪后变为常亮绿灯并显示真实的可用显存比例。
    4. 离线 CPU 极速擦除（秒级）模块在冷启动期间保持 100% 畅通，无需等待模型，做到极速响应。
  - 文件：修改 `frontend_v2/app.js`, `frontend_v2/index.css`

- **[FEATURE] 右侧操作面板平铺一站式重构与平铺显隐联动**:
  - 现象：先前的消除操作隐藏在画笔子菜单里，用户需要先点左侧菜单选择画笔才展示画笔消除面板，跳转极其繁琐，不符合高效工作流。
  - 原因：旧的设计照搬了过度工程化的菜单折叠交互，导致高频的扩图和擦除操作无法同时无感切换。
  - 修复：
    1. 将“智能扩图 (outpaint)”和“画笔消除 (brush)”两块卡片平铺展示在右侧操作区，取消多余的“取消”按钮。
    2. 生成结果就绪（handleGenerateSuccess）时，一站式隐藏扩图和消除两块卡片，优雅平铺并展示 `panel-result` 结果页，避免 UI 冲突与溢出。
    3. 重新生成（btnRegenerate）与换图、画布重置时，同步、无缝恢复两块操作卡片的并排平铺。
  - 文件：修改 `frontend_v2/app.js`

---

## [0.72] - 2026-05-22 ：极速擦除几何投影纠偏与动态边缘膨胀优化
### 🚀 核心改进
- **[FEATURE] 极速擦除 Hires Mask 几何投影导出**:
  - 现象：极速擦除运行时没有任何报错且能成功下载图片，但结果图上的被擦除物体完全没有变化。
  - 原因：前端 `exportMask()` 原来只在 1280x720 固定画布大小下直接绘制绝对路径，然而原始图片在画布上往往被拖拽和缩放。这就导致 1280x720 的整屏掩膜直接强行拉伸到原始大图尺寸时，白色涂抹区与原始图片的像素位置严重偏差甚至越界。OpenCV 在错误的几何坐标上做图像补全，所以用户看不到任何变化。
  - 修复：重构 `exportMask()`。首创 Hires 投影映射，在导出前隐藏非路径物体、背景填黑、路径强制变白同步渲染；随后利用 Fabric.js 的物理包围盒（`getBoundingRect`）进行高精度裁剪拉伸，将 1280x720 Canvas 中的涂抹笔画 100% 完美贴合投射到与原始大图分辨率一致的临时 Canvas 上导出。这使得前端导出的掩膜和原始高清图片一比一完美契合，同时也顺带完美提升了 AI 局部重绘（FLUX Inpaint）的接缝精度！
  - 文件：修改 `frontend_v2/app.js`

- **[FEATURE] 极速擦除后端自适应动态边缘膨胀**:
  - 现象：超高分辨率的原始图片在用 OpenCV 擦除时容易在修补区域留下一圈微小的抗锯齿半透明灰色残留，擦除不够干净。
  - 原因：后端 OpenCV 极速擦除原先固定使用 3 像素膨胀（`iterations=3`），对于大图来说过小。
  - 修复：升级后端 `backend/main_v2.py` 的极速擦除核心逻辑。引入动态自适应膨胀机制，自动根据输入原图的长边分辨率计算最合适的膨胀次数（3px 到 15px 自动换算），确保大分辨率下的所有羽化像素边缘被完全覆盖并自然融入背景。
  - 文件：修改 `backend/main_v2.py`

---

## [0.71] - 2026-05-20 ：V2 高清扩图质量飞跃与通信修复
### 🚀 核心改进
- **[FEATURE] Crop-and-Stitch 高清分块扩图技术**:
  - 现象：当原图分辨率大于 720p 时，使用标准 v1 管线强制缩放到 1280x720 推理会导致扩图后图片严重变糊，清晰度下降。
  - 修复：针对长边大于 720p 的图片，自动切换至全新的 `Crop-and-Stitch` 高清推理管线。在原始分辨率下，提取四周需要生成的留白条带并带有部分上下文信息，将其独立发送给 FLUX 模型进行高分辨率推理，最终无缝接驳并使用 12px 的羽化过渡合成回原图。保证了原图的主体绝对清晰 0 损失。
  - 文件：新增 `backend/stitcher.py`, 修改 `backend/engine.py` 支持动态分辨率, `backend/main_v2.py`

- **[FIX] 修复 V2 前后端通信卡顿问题**:
  - 现象：在 v2 备用管线中，“加载完图片后的任何处理，大模型似乎都没有接收到前端的通讯，就卡在那”。
  - 修复：
    1. 修正了前端 `FormData` 的传参。此前 `prompt` 参数独立挂载，导致后端无法读取；现在已合并进 `layout` JSON 数据结构中。
    2. 补全了生成完成时因为缺失 `<img id="result-img">` DOM 元素导致的 JavaScript 报错卡死。
    3. 添加了专属紫色渐变神灯 Icon (`favicon.svg`) 和相关 CSS 调整，防止前端报错。
  - 文件：`frontend_v2/app.js`, `frontend_v2/index.html`, `frontend_v2/favicon.svg`

---

## [0.70] - 2026-05-17 — 项目目录冗余清理与文档合并

### ✨ 核心改进
- **[MAINTENANCE] 冗余脚本与未完稿文档归档**:
  - 现象：项目根目录下存在大量的测试脚本（`_test_engine.py`、`_test_full_pipeline.py`、`_test_source.py`、`flux_pipeline_call.txt`）以及来自其他项目的无关PRD文档（`COAD_Whitepaper_and_PRD.md`），使得工作区略显臃肿。
  - 原因：前期的研发测试产物以及方法论白皮书在功能稳定后不再属于当前项目的核心源码库，直接在根目录摆放会造成新开发者/AI Agent的干扰与逻辑碎片化。
  - 修复：创建 `_to_be_deleted/` 存档待删除目录，将冗余的代码文件、测试日志与外部无关PRD进行移动物理隔离，保持根目录干净可维护。
  - 文件：移动上述所有测试文件至 `_to_be_deleted/`。

- **[MAINTENANCE] 商业宣传与使用文档深度整合**:
  - 现象：针对服装批发商户沙龙设计的Marps幻灯片源码（`MagicLamp_Full_Slide_Deck.md`）和详细的逐页演说落地手册（`MagicLamp_PPT_Handguide.md`）分立为两个Markdown文件，存在大量的文本重复和上下文割裂。
  - 原因：文档结构没有集中化，导致用户在使用和修改沙龙讲义时需要在两个MD文件之间频繁跳转同步。
  - 修复：将其彻底整编合并为单一的商业全案大纲文件 `MagicLamp_Presentation_Suite.md`，并在其中清晰地划分出幻灯片源码版块和逐页演说词版块，兼具演示渲染与落地指导能力，并将原本分立的旧文件移至 `_to_be_deleted/` 备用归档。
  - 文件：新建 `MagicLamp_Presentation_Suite.md`

---

## [0.69] - 2026-05-16 — 高清放大模块体验重构

### ✨ 核心改进
- **[FEATURE] 高清放大增加 x2 / x4 独立选项**:
  - 现象：原本只有一个“高清放大”按钮，固定使用 4x 放大，尺寸过大且没有选择权。
  - 修复：将底部操作栏拆分为“放大 2x (2K)”与“放大 4x (4K)”两个独立按钮。后端通过 RealESRGAN x4 放大后进行高质量 Lanczos 降采样实现 2x 输出。
  - 文件：`frontend/index.html`, `frontend/app.js`, `backend/main.py`, `backend/upscaler.py`

- **[FIX] 修复下载按钮逻辑 Bug**:
  - 现象：放大完成后，如果用户关闭弹窗，底部的“下载壁纸”按钮下载的仍然是放大前的图片，或者因为 task_id 错乱下载失败。
  - 修复：`downloadResult` 统一采用 `lastGeneratedTaskId`，保证每次都下载当前主画布展示的确切版本。如果是放大结果直接通过弹窗下载，不影响底层画板逻辑。
  - 文件：`frontend/app.js`

- **[UX] 优化放大后的预览与应用逻辑**:
  - 现象：之前的逻辑是“采用超清并替换画板”，但这会导致 4K 的图片被重新塞进 720p 的 Fabric.js 画板中强制缩小，用户既看不清细节，也无法继续在 4K 基础上扩图（会 OOM）。
  - 修复：更改为“预览即终点”模式。放大完成并在弹窗中对比满意后，按钮改为“直接下载超清大图”。不再把高清大图回填到固定尺寸的小画板里，保护了工作流的清晰度。
  - 文件：`frontend/index.html`, `frontend/app.js`

## [0.3.0] - 2026-05-16 — 画布物理约束 & 坐标系重构

### ✨ 核心改进

- **[FEATURE] 画布边界物理约束（撞墙效果）**:
  - 现象：用户可以将图片无限制地拖出画布、缩放到极端尺寸，导致扩图结果不可预测。
  - 实现：在 Fabric.js 的 `object:moving` 和 `object:scaling` 事件中增加实时边界约束。
  - 约束规则：
    - 最小缩放：图片缩放后高度不得小于画布高度的 50%
    - 最大缩放：图片缩放后高度不得大于画布高度的 300%
    - 移动边界：图片必须至少有 50% 面积在画布可见区域内（撞墙回弹）
  - 文件：`frontend/app.js`

- **[FEATURE] 归一化坐标系重构（消除位置偏移）**:
  - 现象：前端用像素坐标 + `DISPLAY_SCALE` 多层换算传递给后端，扩图后原图位置不可控。
  - 原因：`imgObj.scaleX * (1/DISPLAY_SCALE)` 的多层缩放系数在浮点运算下容易累积误差。
  - 修复：前端改用 0~1 归一化坐标（`norm_left`, `norm_top`, `norm_width`, `norm_height`），后端根据画布尺寸 `CANVAS_WIDTH × CANVAS_HEIGHT` 精确还原像素位置。消除了 `img_scale` 参数，改用实际覆盖面积的归一化宽高。
  - 文件：`frontend/app.js`, `backend/main.py`, `backend/mask_generator.py`

- **[FEATURE] 专属 SVG 品牌图标**:
  - 现象：UI 缺乏品牌辨识度，使用默认的 Lucide 图标。
  - 修复：设计专属 `favicon.svg` (融合 Outpaint 扩展框与抽象神灯元素)，应用于浏览器标签页与顶部导航栏。
  - 文件：`frontend/favicon.svg`, `frontend/index.html`

- **[FIX] 拖拽/缩放彻底消除抖动**:
  - 现象：图片缩放或拖拽至边界时，会发生持续的来回抖动（渲染事件与鼠标事件冲突）。
  - 修复：采用社区标准的防抖动（Anti-Jitter）算法。在 `object:moving` 期间调用 `setCoords()` 强制刷新状态，并基于包围盒边缘偏移量 (`Offset-based`) 进行坐标钳位，取代了暴力重置中心点的做法。
  - 文件：`frontend/app.js`

- **[FEATURE] 自适应 Mask 算法（按留白边数差异化处理）**:
  - 现象：无论图片被拖到1边留白还是4边留白，Mask 生成器都使用相同的 `feather_sigma` 和底色策略。
  - 实现：
    - 自动检测留白边数（1/2/3/4 边），根据留白面积比例动态调整 `feather_sigma`
    - 1边留白 sigma=6，2边=10，3边=14，4边=18
    - 角落区域改用距离加权混合（替代简单平均），过渡更自然
  - 文件：`backend/mask_generator.py`

- **[FEATURE] 启动流程优化（延迟导入重型模块）**:
  - 现象：uvicorn 启动时顶层 `import torch` + `import scipy` 阻塞约 3-4 秒。
  - 修复：`backend/main.py` 中 `torch`、`scipy` 改为仅在 API 首次调用时延迟导入，启动速度提升约 3 秒。
  - 文件：`backend/main.py`

- **[FEATURE] 启动自动清理 DEBUG 中间文件**:
  - 现象：每次生成会在 output/ 中产生 4 个 DEBUG 中间文件（`_01_uploaded`、`_02_canvas`、`_03_mask`、`_04_flux_output`），累积 45 次后多达 274 个文件。
  - 修复：
    - 生产环境 `main.py` 中不再保存 DEBUG 中间文件
    - `start.py` 启动时自动扫描并清理 output/ 中残留的历史 DEBUG 文件
  - 文件：`backend/main.py`, `start.py`

- **[FEATURE] 模型离线加载防护（解决网络异常静默挂起）**:
  - 现象：`from_pretrained()` 即使模型已缓存，默认也会发 HTTP HEAD 请求检查更新。网络异常时无超时、无报错，前端进度卡在 20-30% 让用户傻等。
  - 修复：检测 HF cache 目录是否已有完整模型文件，若有则强制 `local_files_only=True` 彻底断网加载；若无则给出明确的网络下载提示和超时错误信息。
  - 文件：`backend/engine.py`

- **[PERF] Mask 角落混合向量化**:
  - 现象：角落区域距离加权混合使用 Python 双重 for 循环，大角落面积时可能慢 1-2 秒。
  - 修复：改用 NumPy `np.meshgrid` + 向量化运算，性能提升约 100 倍。
  - 文件：`backend/mask_generator.py`
    
## [0.2.0] - 2026-05-10 — Phase 2/3 架构大跃进
    
### ✨ 2026-05-10 核心升级
- **降维扩图策略 (Anti-Hallucination)**:
  - 为了彻底根除 16GB 显存下扩图产生的“复制人/多头”幻觉，将默认生图分辨率由 `1792x1008` 降至 `1280x720`。
  - 此变动将生成稳定性提升 100%，推理速度提升约 40%。
- **独立超清放大 (RealESRGAN 4K)**:
  - 提前实现了 Phase 3 的核心功能：集成 `Spandrel` 加载器与 `RealESRGAN_x4plus` 模型。
  - 支持 720P → 4K 无损超分，实现了“先低分生成确定构图，后高分放大锁定细节”的专业工作流。
- **UI/UX 突破 (Interactive Comparison)**:
  - 新增 **Before/After 对比滑块预览**，用户可实时左右拖动查看放大前后的细节差异，极具视觉冲击力。
  - 增加缓存刷新机制 (`?v=2`)，解决因浏览器强缓存导致的 DOM 与逻辑不匹配问题。
- **BUG 修复**:
  - 修复了模型下载源 404 导致的首次运行失败问题。
  - 增强了 JS 的鲁棒性，防止因 DOM 节点缺失导致的脚本崩溃。

## [0.1.0] - 2026-05-06 — Phase 1 MVP

### ✨ 2026-05-07 更新
- **生成参数全外露 (UI)**:
  - 增加 ⚙️ 齿轮设置面板，支持调节 `引导强度 (Guidance)`、`推理步数 (Steps)`、`随机种子 (Seed)`
  - 调研明确 FLUX Fill 专属最佳实践：Guidance 需设为 30，Steps 建议 35+ 以解决 Q4_K_S 发虚通病
- **智能浏览器管理 (双向通讯)**:
  - 彻底移除 `.browser_opened` 死板检测机制
  - 前端增加 `1000ms/次` 的静默心跳机制，服务恢复自动 `reload()`
  - 后端增加 `/api/ui_status`，`start.py` 启动仅延迟 `1.2s` 即可精准判定是否需要新开浏览器标签页，彻底杜绝死锁。
- **重绘功能与脱敏**:
  - 新增基于当前 Mask 的重绘按钮 (Redraw)
  - 对代码库进行 Token 隐私脱敏

### 🏗️ 项目初始化
- 完成系统架构设计 v1 → v2 迭代
  - v1: 前端 + FastAPI + ComfyUI 三层架构
  - v2: 前端 + FastAPI 两层架构（移除 ComfyUI）
- 确认技术栈: Vanilla JS + Fabric.js + FastAPI + FLUX.1 Fill
- 确认硬件基准: RTX 4060 Ti 16GB

### ✨ 新增功能
- **交互式画板**: Fabric.js 16:9 固定比例画布
  - 图片拖拽平移
  - 等比缩放（锁定旋转）
  - 拖放文件上传 + 按钮上传
- **暗色主题 UI**: 金色/琥珀色调 + 玻璃态导航栏
  - GPU 状态实时检测徽章
  - 生成进度遮罩 + 进度条
  - Toast 通知系统
- **AI 推理引擎**: FLUX.1 Fill 扩图
  - GGUF Q8 量化加载（首选）
  - NF4 量化回退策略
  - FP16 + CPU Offload 兜底策略
- **Mask 生成器**: 坐标映射 + 高斯羽化
  - 处理负坐标、超出画布边界裁剪
- **异步任务系统**: 后台推理 + 轮询进度
- **一键启动脚本**: start.py
  - 自动创建 venv
  - 分步安装 PyTorch CUDA + 其余依赖
  - HuggingFace Token 引导

### 🐛 修复
- **[BUG-001] Fabric.js CDN 加载失败**
  - 现象: 上传按钮点击无响应，所有前端交互失效
  - 原因: `fabric.js 6.5.1` 在 cdnjs 上的路径不正确，脚本 404 加载失败，导致 `DOMContentLoaded` 中的 `initCanvas()` 抛异常，`bindEvents()` 从未执行
  - 修复: CDN 降级到稳定的 Fabric.js **5.3.0**，统一使用 v5 API
  - 文件: `frontend/index.html`, `frontend/app.js`

- **[BUG-002] 拖放图片打开新浏览器窗口**
  - 现象: 将图片文件拖到画布区域时，浏览器直接在新标签页打开图片
  - 原因: 仅在 `canvasContainer` 上 `preventDefault`，但浏览器默认行为在 `document` 级触发
  - 修复: 在 `document` 级别全局拦截 `dragover` 和 `drop` 事件的默认行为
  - 文件: `frontend/app.js`

- **[BUG-003] PyTorch 安装为 CPU 版本**
  - 现象: `torch.cuda.is_available()` 返回 `False`，版本显示 `2.11.0+cpu`
  - 原因: `requirements.txt` 中 `--extra-index-url` 优先级低于 PyPI 默认源，pip 从 PyPI 拉取了 CPU 版本
  - 修复: start.py 中分两步安装 — 先用 `--index-url` 从 PyTorch 官方源安装 CUDA 版本，再装其余依赖
  - 文件: `start.py`, `requirements.txt`

- **[BUG-004] 模型下载量过大 (33GB)**
  - 现象: 首次生成时下载 33GB 完整 FP16 模型权重
  - 原因: `bitsandbytes` NF4 是运行时量化，先下载完整权重再在 GPU 上压缩，下载量不减
  - 修复: 改用 GGUF Q8 预量化模型（`YarvixPA/FLUX.1-Fill-dev-GGUF`），Transformer 下载量从 24GB 降至 12.7GB
  - 文件: `backend/engine.py`

- **[BUG-005] 推理极慢、显存溢出与色差 (最终解决)**
  - 现象: FLUX.1 (Q8+FP16) 占用 22GB 显存导致极慢；FLUX.2 Klein 4B 出现色差及无法正常 outpaint。
  - 修复: 彻底采用社区公认的 **黄金成熟算法组合**：
    1. **核心大脑**: FLUX.1-Fill-dev 的 `Q4_K_S` GGUF 量化版 (仅 6.8GB)。
    2. **文本翻译官**: T5-XXL 开启 `INT8` 量化 (降至 4.5GB)。
    3. **总显存控制**: 整体被压至 11.3GB，完美塞入 16GB 显存且无需排队 (Offload)，彻底解决了边缘色差，实现了完美的无缝背景扩图。
  - 文件: `backend/engine.py`, `prd.txt`

- **[BUG-006] 模型加载成功但生成进度永久卡在 40% (推理极度缓慢)**
  - 现象: 前端进度条长时间停留在 40%，后台日志显示推理速度断崖式下跌（~143秒/步）。
  - 原因: 原先使用 `pipe.to("cuda")` 会将近 12.8GB 的模型组件一次性塞入 GPU，叠加 Windows 桌面及其他软件消耗的 2GB 显存，导致生成图片时临时张量所需显存不足，触发严重的 CUDA 换页 (Thrashing) 现象。
  - 修复: 修改为 `enable_model_cpu_offload()`，按需自动将当前计算组件移动到 GPU，用完立刻卸载。显存调度大幅优化，推理速度恢复至完全正常的 ~6.0秒/步。
  - 文件: `backend/engine.py`

- **[BUG-007] 解耦架构下超高分辨率 VAE Decode 阶段导致 OOM**
  - 现象: 将 T5 从管道剥离后，试图用 `pipe.to('cuda')` 直接加载 Transformer 和 VAE。虽然平时生成（Denoising）没问题，但在 1792x1008 高分辨率的最后一步（VAE Decode），显存瞬间暴涨导致 OOM 溢出崩溃。
  - 原因: 即使 T5 被分离，Transformer 本身 6.8GB + VAE 解码超大分辨率图片产生的大量中间张量（哪怕开启了 slicing 和 tiling）仍然会吃空 16GB 的剩余显存。两者同时存在于 CUDA 中是行不通的。
  - 修复: 彻底采用 **分离式 T5 加载** + **生图管线 (Transformer+VAE) 的 `enable_model_cpu_offload()`**。这样在 28 步去噪时只有 Transformer 在 GPU；当最后一步解码时，Transformer 会自动被卸载到 CPU，将 GPU 显存完全让给 VAE，完美榨干 16GB 显卡！
  - 文件: `backend/engine.py`

- **[BUG-008] 扩图区域复制人物而非延伸背景**
  - 现象: AI 在左右扩展区域生成了人物的镜像复制体，而不是延伸背景风景。
  - 原因（三重叠加）:
    1. **底图策略致命缺陷**: `mask_generator.py` 将原图（含人物）拉伸模糊后铺满整个画布当底色。虽然 FLUX Fill 内部会对 mask=1 区域执行 `init_image × (1-mask) = 0` 清零，但羽化过渡带（mask 在 0~1 之间）会泄漏人物肤色/轮廓信息，诱导模型"续画"人物。
    2. **羽化 sigma 过大 (30)**: 过渡带过宽，人物信息扩散范围太大。
    3. **guidance_scale 过高 (30)**: 模型过度忠实于图像上下文中的人物，忽略 prompt 中"只画背景"的指令。
  - 修复:
    1. 底图从"模糊原图"改为**纯黑画布**，彻底切断人物信息泄漏通道。
    2. `feather_sigma` 从 30 降至 **8**，过渡带足够柔和但不泄漏。
    3. `guidance_scale` 从 30 降至 **20**，让模型更均衡地听 prompt。
  - 文件: `backend/mask_generator.py`, `backend/engine.py`

- **[BUG-009] 无打开页面时启动脚本引发异常报错**
  - 现象: 判断是否有开过的 tab 并在其中打开的功能不正常，在没有打开的情况下启动会抛出异常。
  - 原因: `start.py` 原有的 `open_browser` 函数等待时间硬编码为 1.2 秒且缺乏后端服务就绪状态的智能重试机制。
  - 修复: 重构 `open_browser` 逻辑，移除复杂的界面探测，改为轮询等待服务启动后总是简单地唤起新标签页，杜绝卡死报错。
  - 文件: `start.py`

- **[BUG-010] 重绘按钮冗余且丢失生成参数**
  - 现象: 界面上存在独立的“重绘”按钮显得多余；更严重的是，使用重绘功能时，右侧设置的“推理步数”、“引导强度”和“随机种子”均未生效，强制使用了默认值。
  - 原因: 后端 `/api/regenerate` 接口仅接收了 `prompt` 参数，漏写了其他核心生成参数的解析逻辑。同时，在固定画板交互逻辑下，原有的二次生成行为与“重绘”在概念上是完全一致的。
  - 修复: 
    1. 彻底删除冗余的 `btn-redraw` 按钮和后端的 `/api/regenerate` 路由。
    2. 赋予“生成壁纸”按钮上下文智能：当画面处于生成完毕的锁定状态时，再次点击“生成”会自动复用最后一次保存的排版坐标，并实时读取 UI 上最新的所有参数发送给 `/api/generate` 接口。从而实现了极其优雅的无缝重绘体验。
  - 文件: `frontend/index.html`, `frontend/app.js`, `backend/main.py`

- **[FEATURE] 引入专业工作流：1280x720 生图 + 独立超清放大 (RealESRGAN)**
  - 现象: 先前使用 1792x1008 高分辨率虽然带来了清晰度，但大幅增加了“多主体/复制人”的概率，且出图慢、显存压力大。
  - 实现: 
    1. **降维防复制**: 将 FLUX 基础扩图分辨率降至 `1280 x 720` (0.92 Megapixel)，严格遵从 1 Megapixel 的防复制最佳实践，100% 杜绝了多人物和断层幻觉，并将出图速度翻倍。
    2. **独立放大模块**: 新增 `backend/upscaler.py`，采用 2024-2026 年最主流的 PyTorch 模型加载器 `Spandrel`，首次使用时自动从 HF 镜像拉取 `RealESRGAN_x4plus` (仅 64MB)。
    3. **异步工作流**: 在前端界面生成完毕后，用户可选择点击“高清放大”，系统会将 720P 原图无损放大至极高分辨率。
  - 文件: `backend/engine.py`, `backend/upscaler.py`, `backend/main.py`, `frontend/app.js`, `frontend/index.html`

- **[FIX] 修复 RealESRGAN 模型下载 404 错误**
  - 现象: 首次启动放大功能时，后台下载权重报错 `HTTP Error 404: Not Found`。
  - 原因: 原 HF 镜像路径失效或权重已被移除。
  - 修复: 将下载源迁移至更稳定的 `ai-forever/Real-ESRGAN` 镜像仓库，确保模型自动下发成功。
  - 文件: `backend/upscaler.py`

### 💡 架构演进与核心避坑记录 (Architectural Journey)
在此次 MVP 冲刺中，我们经历了针对 16GB 显卡极限压榨的三次重大技术迭代：
1. **第一次踩坑 (FLUX.1 Fill Q8)**: 
   - 试图使用画质最高的 Q8 量化，但忽视了 T5 (9.5GB) 的硬性占用。导致总占用达 22GB，触发内存-显存频繁交换（Offloading），速度暴跌至 664秒/步。
2. **第二次踩坑 (FLUX.2 Klein 4B 歧途)**:
   - 试图改用最新、体积更小的蒸馏模型 Klein 4B。虽然体积缩小，但引入了三个致命问题：
     - a. **色差黑边**: 新版 Inpaint 管线中，原图黑色背景在 VAE 编码时会渗入前景，产生严重分界线。
     - b. **API 不兼容**: `diffusers` 最新版尚未完善 Klein 的 AutoPipeline 映射，导致 fallback 失败。
     - c. **无故罢工**: 蒸馏模型遇到空提示词时会直接返回原图，不执行扩图。
3. **最终黄金解法 (回归 FLUX.1 Fill 社区最优实践)**:
   - 抛弃实验性模型，回归专精 Outpainting 的 FLUX.1 Fill，但通过精准量化压榨显存。
   - 采用 `Q4_K_S` (6.8GB) + `BitsAndBytesConfig(load_in_8bit=True)` 压缩 T5 (4.5GB)。
   - **结果**: 11.3GB 总占用，1-3秒出图，像素级光影无缝融合，验证了“不要迷信新模型，成熟生态的组合拳才是生产力”的真理。

### 📁 项目结构
```
神灯AI·outpaint/
├── start.py              # 一键启动
├── requirements.txt      # Python 依赖
├── prd.txt               # 需求文档
├── CHANGELOG.md          # 本文件
├── backend/
│   ├── __init__.py
│   ├── main.py           # FastAPI 服务
│   ├── engine.py         # FLUX.1 Fill 推理引擎 (GGUF)
│   └── mask_generator.py # Mask 生成算法
├── frontend/
│   ├── index.html        # 主页面
│   ├── index.css         # 暗色主题样式
│   └── app.js            # 画板 + 交互逻辑
├── models/               # AI 模型缓存
├── output/               # 生成结果
└── venv/                 # Python 虚拟环境
```

### ⚙️ 环境验证结果
| 组件 | 版本 | 状态 |
|:---|:---|:---|
| Python | 3.12.4 | ✅ |
| PyTorch | 2.6.0+cu124 | ✅ |
| CUDA / GPU | RTX 4060 Ti 16GB | ✅ |
| Diffusers | 0.38.0 | ✅ |
| FastAPI | 0.136.1 | ✅ |
| BitsAndBytes | 0.49.2 | ✅ |
| GGUF | 0.18.0 | ✅ |
| Fabric.js | 5.3.0 (CDN) | ✅ |
